# Лендинг для wellness-продуктов

## Обзор проекта
Минималистичный лендинг для wellness-продуктов с чистым дизайном. Цель — мотивировать посетителей перейти на основной сайт каталога. Проект создан на React + Vite + TypeScript + Tailwind CSS + Framer Motion.

**Дизайн-философия**: Экстремальный минимализм. Секции разделяются только цветом фона — никаких декоративных элементов.

## Последние изменения (21 декабря 2025)

### Циклическая карусель отзывов - ИСПРАВЛЕНИЯ
Реализована правильная бесконечная карусель с полным анализом и корректировкой:

**Проблемы и решения:**
- **Проблема**: Gap-6 между элементами не учитывался в расчете смещения, из-за чего видна была часть третьего комментария
- **Решение**: 
  - Создан расширенный массив: `[...testimonials, testimonials[0], testimonials[1]]` (дубли первых 2 в конце)
  - Рассчитано смещение: **52%** вместо 50% (50% за ширину элемента + ~2% компенсация gap)
  - Логика цикла: `(prev + 1) % testimonials.length` (вправо), `(prev - 1 + testimonials.length) % testimonials.length` (влево)
- **Результат**: Истинная циклическая карусель 1→2→3→4→5→6→1 без прыжков и обрезаний

**Технические детали карусели:**
- Контейнер: `w-full overflow-hidden` (скрывает лишние элементы)
- Элементы: `flex-shrink-0 w-1/2` (каждый 50% ширины)
- Анимация: `x: -${testimonialIndex * 52}%` (с компенсацией gap)
- Framer Motion: `spring` с `stiffness: 300, damping: 30` (плавный переход)

---

## Структура продуктов
5 основных товаров:
1. **Пластырь** - для точечного воздействия
2. **Очиститель воды** - структурирует и очищает воду
3. **Очелье** - налобная повязка для головы
4. **Наочники** - для глаз и зрения
5. **Кушак** - пояс для спины и живота

## Секции лендинга
- **Hero** - главный баннер с историей исцеления и CTA
- **Как это работает** - 3 шага с иллюстрацией
- **Каталог продуктов** - зигзаг layout с карточками (зеленоватый фон)
- **Отзывы клиентов** - 6 отзывов с циклической каруселью (показывает по 2)
- **FAQ** - 5 вопросов-ответов с аккордеоном
- **CTA** - финальный призыв к действию

## Разделение секций по цвету фона
```
Hero        → bg (#f8faf8)
How It Works → bg (#f8faf8)
Products    → catalogBg (#f4f9f5) - зеленоватый
Testimonials → bg (#f8faf8)
FAQ         → bgAlt (#eef4ee) - чуть темнее
CTA         → gradient (зелёный градиент)
```

## Архитектура проекта

### Структура файлов
```
/
├── src/
│   ├── components/
│   │   └── HeroVariants.tsx      # Hero блок "История Исцеления"
│   ├── pages/
│   │   └── DesignVariants.tsx    # Главная страница (минималистичная)
│   ├── context/
│   │   └── PaletteContext.tsx    # Контекст для цветов
│   ├── App.tsx                   # Роутинг приложения
│   ├── main.tsx                  # Точка входа React
│   └── index.css                 # Глобальные стили
├── index.html                    # HTML шаблон
├── vite.config.ts                # Конфигурация Vite
├── tailwind.config.ts            # Конфигурация Tailwind
├── package.json                  # Зависимости проекта
└── tsconfig.json                 # Настройки TypeScript
```

### Технологический стек
- **React 19** - UI библиотека
- **TypeScript** - типобезопасность
- **Vite** - быстрый сборщик и dev сервер
- **Tailwind CSS v4** - utility-first CSS фреймворк
- **Framer Motion** - анимации и переходы
- **Wouter** - лёгкий роутинг
- **Lucide React** - иконки

### Конфигурация
- **Dev сервер**: порт 5000, host 0.0.0.0
- **Production build**: статический сайт в папке `dist/`
- **HMR**: настроен для работы через WebSocket Secure (wss://)

## Команды
- `npm run dev` - запуск dev сервера (порт 5000)
- `npm run build` - сборка для production
- `npm run preview` - предпросмотр production сборки

## Цветовая палитра
```javascript
colors = {
  bg: "#f8faf8",           // Основной фон
  bgAlt: "#eef4ee",        // Альтернативный фон (FAQ)
  accent: "#3d6b4f",       // Акцентный зелёный
  accentLight: "#d4e5d8",  // Светло-зелёный
  accentDark: "#2a4a36",   // Тёмно-зелёный
  text: "#1a2e1f",         // Основной текст
  textSecondary: "#4a6b52", // Вторичный текст
  cardBg: "#ffffff",       // Фон карточек
  catalogBg: "#f4f9f5",    // Зеленоватый фон каталога
}
```

## Следующие шаги
1. Заменить placeholder изображения на реальные фото продуктов
2. Обновить тексты описаний продуктов
3. Добавить реальные отзывы клиентов
4. Настроить ссылки CTA на основной сайт
5. Настроить SEO meta-теги
6. Добавить аналитику (GA, Yandex Metrika)
